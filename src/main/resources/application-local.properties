# Datasource config
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/wordcount?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=1@mr00t

# Flyway config
spring.flyway.driver-class-name=com.mysql.cj.jdbc.Driver
spring.flyway.url=jdbc:mysql://localhost:3306?serverTimezone=UTC
spring.flyway.user=root
spring.flyway.password = 1@mr00t
spring.flyway.schemas = wordcount


# counter
api.word.count.mode = CONCURRENT_CHUNKED
api.word.count.dir.downloads = downloads

api.word.count.chunked.buffer.bytes.size=1048576
api.word.count.chunked.word.maxLength=100

logging.level.org.springframework=INFO